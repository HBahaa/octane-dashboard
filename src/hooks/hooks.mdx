import { Meta } from '@storybook/blocks';

<Meta title="Custom Hooks/Docs" />

#### Why are we using it ?

* To insure that this logic is applied/used in the same way across the app.
* To reduce the cost of change in logic to be in one place

#### Our Custom Hooks Include :

* "useMobile" : returns boolean, if screen isMobile.
